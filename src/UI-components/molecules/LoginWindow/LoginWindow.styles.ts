import { styled } from '../../../theme/styled';

export const Section = styled('section')(({ theme }) => ({
    width: '302px',
    // height: '314px',
    position: 'relative'
    // alignSelf: 'center',
}));